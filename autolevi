local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

task.spawn(function()
    pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("SetTeam", "Marines") end)
    pcall(function()
        if getrawmetatable and newcclosure then
            local rawMeta = getrawmetatable(game)
            if rawMeta then
                if setreadonly then setreadonly(rawMeta, false) end
                local oldNamecall = rawMeta.__namecall
                rawMeta.__namecall = newcclosure(function(self, ...)
                    local method = getnamecallmethod and getnamecallmethod()
                    if method == "FireServer" or method == "InvokeServer" then
                        local nameStr = tostring(self)
                        if nameStr:find("Ban") or nameStr:find("Check") or nameStr:find("Admin") or nameStr:find("Detection") then return nil end
                    end
                    if method == "Kick" or method == "kick" then return nil end
                    return oldNamecall(self, ...)
                end)
                if setreadonly then setreadonly(rawMeta, true) end
            end
        end
    end)
end)

task.spawn(function()
    pcall(function()
        Lighting.FogEnd = 999999
        Lighting.GlobalShadows = false
        Lighting.Brightness = 2
        for _, v in pairs(Lighting:GetChildren()) do
            if v:IsA("Atmosphere") or v:IsA("PostEffect") or v:IsA("Sky") then v:Destroy() end
        end
    end)
end)

_G.Config = {
    IsMaster = false, 
    IsSlave = false, 
    MasterName = "bonbicuttay_999",
    WaterWalk = true,
    AutoBuso = true,
    AutoV3 = false,
    AutoV4 = false,
    AutoAvoidSeaEvent = true,
    SelectedBoat = "Dinghy",
    AutoGetSanguineArt = false,
    AutoPullHeartToTiki = false,
    AutoPullHeartToHydra = false,
    AutoTravel = "",
    TweenSpeed = 300,
    BoatTweenSpeed = 350,
    CustomIconId = "rbxassetid://130284374965787"
}

_G.SenakaLocations = {
    TikiBoat = CFrame.new(-16234, 25, 416),
    Hydra = CFrame.new(5749, 610, -282),
    SpyNpc = CFrame.new(-16110, 20, 450),
    DemonicFumeNpc = CFrame.new(5263, 2, -10410),
    SeaSearchCenter = CFrame.new(-16000, 20, -15000)
}

_G.SenakaCharacterMove = function(targetCFrame, speed)
    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not root or not hum then return end

    speed = speed or _G.Config.TweenSpeed
    local noclipConn = RunService.Stepped:Connect(function()
        if char and char.Parent then
            for _, part in pairs(char:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)

    hum.PlatformStand = true
    local distance = (root.Position - targetCFrame.Position).Magnitude
    local duration = distance / speed
    local tween = TweenService:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = targetCFrame})

    tween:Play()
    tween.Completed:Wait()
    if noclipConn then noclipConn:Disconnect() end
    hum.PlatformStand = false
end

local VirtualInputManager = game:GetService("VirtualInputManager")

local function GetPlayerBoat()
    local boatsFolder = workspace:FindFirstChild("Boats")
    if boatsFolder then
        for _, boat in pairs(boatsFolder:GetChildren()) do
            local owner = boat:FindFirstChild("Owner")
            if owner then
                if typeof(owner.Value) == "Instance" and owner.Value == LocalPlayer then return boat end
                if tostring(owner.Value) == LocalPlayer.Name then return boat end
            end
        end
    end
    return nil
end

local lastBuyTime = 0
local function FindAndInteractBoatDealer()
    if tick() - lastBuyTime < 10 then return end
    
    local dealerPart = nil
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("Model") then
            local nameLower = obj.Name:lower()
            if nameLower:find("boat dealer") or nameLower:find("boatdealer") or nameLower:find("dealer") then
                if obj:FindFirstChild("HumanoidRootPart") then
                    dealerPart = obj.HumanoidRootPart
                    break
                end
            end
        end
    end
    
    if not dealerPart then
        for _, obj in pairs(workspace:GetChildren()) do
            if obj:IsA("Model") and obj:FindFirstChild("HumanoidRootPart") and obj:FindFirstChild("Humanoid") then
                local dist = (obj.HumanoidRootPart.Position - _G.SenakaLocations.TikiBoat.Position).Magnitude
                if dist < 300 then
                    dealerPart = obj.HumanoidRootPart
                    break
                end
            end
        end
    end

    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if dealerPart and root then
        if (dealerPart.Position - root.Position).Magnitude > 10 then
            _G.SenakaCharacterMove(dealerPart.CFrame * CFrame.new(0, 3, 3), _G.Config.TweenSpeed)
            task.wait(0.5)
        end
    end

    pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("BuyBoat", _G.Config.SelectedBoat or "Dinghy") end)
    lastBuyTime = tick()
    task.wait(3)
end
local processedBoats = {}
local function SecureBoatPhysics(boatModel)
    if not boatModel or processedBoats[boatModel] then return end
    processedBoats[boatModel] = true
    task.spawn(function()
        task.wait(0.5)
        pcall(function()
            for _, part in pairs(boatModel:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = true end
            end
        end)
    end)
end

local function CountPlayersOnBoat(boatModel)
    if not boatModel then return 0 end
    local count = 0
    local countedPlayers = {}
    for _, desc in pairs(boatModel:GetDescendants()) do
        if desc:IsA("Seat") or desc:IsA("VehicleSeat") then
            if desc.Occupant and desc.Occupant.Parent then
                local player = Players:GetPlayerFromCharacter(desc.Occupant.Parent)
                if player and not countedPlayers[player] then
                    countedPlayers[player] = true
                    count = count + 1
                end
            end
        end
    end
    return count
end

local WaterPlatform = Instance.new("Part")
WaterPlatform.Name = "SenakaWaterSurfacePlatform"
WaterPlatform.Size = Vector3.new(100, 1, 100)
WaterPlatform.Anchored = true
WaterPlatform.Transparency = 1
WaterPlatform.CanCollide = false
WaterPlatform.Parent = workspace

RunService.RenderStepped:Connect(function()
    if _G.Config.WaterWalk then
        pcall(function()
            local char = LocalPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChild("Humanoid")
            
            if root and hum and not hum.SeatPart then
                if root.Position.Y <= 10 and root.Position.Y >= -30 then
                    WaterPlatform.CFrame = CFrame.new(root.Position.X, -2.5, root.Position.Z)
                    WaterPlatform.CanCollide = true
                else
                    WaterPlatform.CanCollide = false
                end
            else
                WaterPlatform.CanCollide = false
            end
        end)
    else
        WaterPlatform.CanCollide = false
    end
end)

task.spawn(function()
    while task.wait(1) do
        if _G.Config.AutoBuso then
            pcall(function()
                local char = LocalPlayer.Character
                if char and not char:FindFirstChild("HasBuso") then ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso") end
            end)
        end
    end
end)

task.spawn(function() 
    while task.wait(0.5) do 
        if _G.Config.AutoV3 then 
            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("ActivateAbility") end) 
        end 
    end 
end)

task.spawn(function() 
    while task.wait(0.8) do 
        if _G.Config.AutoV4 then 
            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("ActivateRaceTransformation") end) 
        end 
    end 
end)

task.spawn(function()
    while task.wait(0.3) do
        if _G.Config.AutoAvoidSeaEvent then
            pcall(function()
                local char = LocalPlayer.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if root then
                    local seaMonsters = {"SeaBeast", "TerrorShark", "Piranha", "ShipRaid", "SeaMonster"}
                    for _, obj in pairs(workspace:GetChildren()) do
                        for _, name in ipairs(seaMonsters) do
                            if obj.Name:find(name) then
                                local monsterPart = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
                                if monsterPart and (monsterPart.Position - root.Position).Magnitude < 300 then
                                    root.CFrame = root.CFrame + Vector3.new(0, 150, 0)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        local Heart = workspace:FindFirstChild("LeviathanHeart")
        local Root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if Heart and Root then
            if _G.Config.AutoPullHeartToTiki then
                Heart:PivotTo(_G.SenakaLocations.TikiBoat + Vector3.new(0, 10, 0))
                pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end)
            elseif _G.Config.AutoPullHeartToHydra then
                Heart:PivotTo(_G.SenakaLocations.Hydra + Vector3.new(0, 10, 0))
                pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end)
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.Config.AutoTravel ~= "" then
            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("Travel", _G.Config.AutoTravel) end)
            _G.Config.AutoTravel = ""
        end
    end
end)
local boatFlightState = "Idle" 

task.spawn(function()
    while task.wait(0.5) do
        local Char = LocalPlayer.Character
        local Hum = Char and Char:FindFirstChild("Humanoid")
        local Root = Char and Char:FindFirstChild("HumanoidRootPart")

        if _G.Config.AutoGetSanguineArt and Root and Hum and Hum.Health > 0 then
            local enemyTarget = nil
            if workspace:FindFirstChild("Enemies") then
                for _, mob in pairs(workspace.Enemies:GetChildren()) do
                    local mobRoot = mob:FindFirstChild("HumanoidRootPart")
                    local mobHum = mob:FindFirstChild("Humanoid")
                    if mobRoot and mobHum and mobHum.Health > 0 then
                        if (mob.Name:find("Vampire") or mob.Name:find("TerrorShark") or mob.Name:find("FishTail") or mob.Name:find("Demonic")) then
                            if (mobRoot.Position - Root.Position).Magnitude < 2000 then
                                enemyTarget = mobRoot
                                break
                            end
                        end
                    end
                end
            end

            if enemyTarget then
                Root.CFrame = enemyTarget.CFrame * CFrame.new(0, 5, 3)
                pcall(function()
                    if VirtualInputManager then
                        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                        task.wait(0.05)
                        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                    end
                end)
            end
        end

        if not _G.Config.AutoGetSanguineArt then
            if _G.Config.IsMaster and Root and Hum and Hum.Health > 0 then
                local IceIsland = workspace:FindFirstChild("LeviathanIceIsland")
                local Heart = workspace:FindFirstChild("LeviathanHeart")
                local distToTiki = (Root.Position - _G.SenakaLocations.TikiBoat.Position).Magnitude
                
                if distToTiki > 150 and not GetPlayerBoat() and not IceIsland then
                    _G.SenakaCharacterMove(_G.SenakaLocations.TikiBoat, _G.Config.TweenSpeed)
                    boatFlightState = "Idle"
                else
                    local myBoat = GetPlayerBoat()
                    if not myBoat then
                        FindAndInteractBoatDealer()
                        boatFlightState = "Idle"
                    else
                        SecureBoatPhysics(myBoat)
                        
                        if not Hum.SeatPart then
                            local targetSeat = nil
                            for _, d in pairs(myBoat:GetDescendants()) do
                                if d:IsA("VehicleSeat") and not d.Occupant then
                                    targetSeat = d
                                    break
                                end
                            end
                            if not targetSeat then
                                for _, d in pairs(myBoat:GetDescendants()) do
                                    if d:IsA("Seat") and not d.Occupant then
                                        targetSeat = d
                                        break
                                    end
                                end
                            end

                            if targetSeat then
                                Hum.PlatformStand = false
                                Root.CFrame = targetSeat.CFrame
                                task.wait(0.3)
                                pcall(function() targetSeat:Sit(Hum) end)
                            else
                                local boatPart = myBoat.PrimaryPart or myBoat:FindFirstChildWhichIsA("BasePart")
                                if boatPart and (Root.Position - boatPart.Position).Magnitude > 15 then
                                    Root.CFrame = boatPart.CFrame + Vector3.new(0, 5, 0)
                                end
                            end
                        else
                            if CountPlayersOnBoat(myBoat) >= 5 then
                                if IceIsland then
                                    local islandPart = IceIsland:FindFirstChild("PrimaryPart") or IceIsland:FindFirstChildWhichIsA("BasePart")
                                    if islandPart then
                                        if (Root.Position - islandPart.Position).Magnitude > 150 then
                                            _G.SenakaCharacterMove(islandPart.CFrame * CFrame.new(0, 20, 0), _G.Config.TweenSpeed)
                                        else
                                            _G.SenakaCharacterMove(_G.SenakaLocations.SpyNpc, _G.Config.TweenSpeed)
                                            task.wait(0.5)
                                            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("FindLeviathan") end)
                                        end
                                    end
                                    if Heart then pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end) end
                                else
                                    local boatPrimary = myBoat.PrimaryPart or myBoat:FindFirstChildWhichIsA("BasePart")
                                    if boatPrimary then
                                        local distanceToSea = (boatPrimary.Position - _G.SenakaLocations.SeaSearchCenter.Position).Magnitude
                                        
                                        if boatFlightState == "Idle" then
                                            local highTarget = CFrame.new(boatPrimary.Position.X, 150, boatPrimary.Position.Z)
                                            local upTween = TweenService:Create(boatPrimary, TweenInfo.new(1.5, Enum.EasingStyle.Linear), {CFrame = highTarget})
                                            upTween:Play()
                                            upTween.Completed:Wait()
                                            boatFlightState = "FlyingToSea"
                                        elseif boatFlightState == "FlyingToSea" then
                                            if distanceToSea > 300 then
                                                local highSeaTarget = CFrame.new(_G.SenakaLocations.SeaSearchCenter.Position.X, 150, _G.SenakaLocations.SeaSearchCenter.Position.Z)
                                                local duration = (boatPrimary.Position - highSeaTarget.Position).Magnitude / (_G.Config.BoatTweenSpeed or 350)
                                                local seaTween = TweenService:Create(boatPrimary, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = highSeaTarget})
                                                seaTween:Play()
                                                seaTween.Completed:Wait()
                                            end
                                            boatFlightState = "Lowering"
                                        elseif boatFlightState == "Lowering" then
                                            local lowSeaTarget = CFrame.new(_G.SenakaLocations.SeaSearchCenter.Position.X, 5, _G.SenakaLocations.SeaSearchCenter.Position.Z)
                                            local downTween = TweenService:Create(boatPrimary, TweenInfo.new(1, Enum.EasingStyle.Linear), {CFrame = lowSeaTarget})
                                            downTween:Play()
                                            downTween.Completed:Wait()
                                            boatFlightState = "Searching"
                                        elseif boatFlightState == "Searching" then
                                            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("FindLeviathan") end)
                                        end
                                    end
                                end
                            else
                                boatFlightState = "Idle"
                            end
                        end
                    end
                end
            end

            if _G.Config.IsSlave and Root and Hum and Hum.Health > 0 then
                local MasterPlayer = nil
                local foundMaster = false
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and (p.Name:lower() == _G.Config.MasterName:lower() or p.DisplayName:lower() == _G.Config.MasterName:lower()) then
                        MasterPlayer = p
                        foundMaster = true
                        break
                    end
                end
                
                if not foundMaster and _G.Config.MasterName ~= "" then
                    for _, p in pairs(Players:GetPlayers()) do
                        if p ~= LocalPlayer then
                            MasterPlayer = p
                            break
                        end
                    end
                end

                if MasterPlayer and MasterPlayer.Character then
                    local MasterRoot = MasterPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if MasterRoot and not Hum.SeatPart then
                        local TargetSeat = nil
                        local masterBoat = GetPlayerBoat()
                        if masterBoat then
                            for _, d in pairs(masterBoat:GetDescendants()) do
                                if (d:IsA("Seat") or d:IsA("VehicleSeat")) and not d.Occupant then
                                    TargetSeat = d
                                    break
                                end
                            end
                        end

                        if TargetSeat then
                            Hum.PlatformStand = false
                            Root.CFrame = TargetSeat.CFrame
                            task.wait(0.3)
                            pcall(function() TargetSeat:Sit(Hum) end)
                        else
                            if (Root.Position - MasterRoot.Position).Magnitude > 20 then
                                Root.CFrame = MasterRoot.CFrame * CFrame.new(0, 0, 5)
                            end
                        end
                    end
                end
            end
        end
    end
end)
pcall(function()
    if CoreGui:FindFirstChild("SenakaCardUIPanel") then CoreGui.SenakaCardUIPanel:Destroy() end
    if CoreGui:FindFirstChild("SenakaSleekModernUI") then CoreGui.SenakaSleekModernUI:Destroy() end
    if CoreGui:FindFirstChild("SenakaNeoCyberUI") then CoreGui.SenakaNeoCyberUI:Destroy() end
    if CoreGui:FindFirstChild("SenakaAzureStyleUI") then CoreGui.SenakaAzureStyleUI:Destroy() end
    if CoreGui:FindFirstChild("SenakaModernAppUI") then CoreGui.SenakaModernAppUI:Destroy() end
    if CoreGui:FindFirstChild("SenakaGlassPanelUI") then CoreGui.SenakaGlassPanelUI:Destroy() end
    if CoreGui:FindFirstChild("SenakaCustomIconUI") then CoreGui.SenakaCustomIconUI:Destroy() end
end)

local targetParent = CoreGui
pcall(function()
    if gethui then
        targetParent = gethui()
    elseif LocalPlayer and LocalPlayer:FindFirstChild("PlayerGui") then
        targetParent = LocalPlayer.PlayerGui
    end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SenakaCustomIconUI"
ScreenGui.Parent = targetParent
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

local ToggleBtn = Instance.new("ImageButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(20, 22, 30)
ToggleBtn.Position = UDim2.new(0, 30, 0, 150)
ToggleBtn.Size = UDim2.new(0, 52, 0, 52)
ToggleBtn.Image = (_G.Config and _G.Config.CustomIconId) or "rbxassetid://130284374965787"

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleBtn

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(0, 220, 130)
ToggleStroke.Thickness = 2
ToggleStroke.Parent = ToggleBtn

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(13, 15, 22)
MainFrame.BackgroundTransparency = 0.05
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -190)
MainFrame.Size = UDim2.new(0, 620, 0, 380)

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(40, 50, 70)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

local TopBar = Instance.new("Frame")
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(18, 21, 30)
TopBar.BorderSizePixel = 0
TopBar.Size = UDim2.new(1, 0, 0, 42)

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 14)
TopBarCorner.Parent = TopBar

local TopBarCover = Instance.new("Frame")
TopBarCover.Parent = TopBar
TopBarCover.BackgroundColor3 = Color3.fromRGB(18, 21, 30)
TopBarCover.BorderSizePixel = 0
TopBarCover.Position = UDim2.new(0, 0, 1, -6)
TopBarCover.Size = UDim2.new(1, 0, 0, 6)

local TitleText = Instance.new("TextLabel")
TitleText.Parent = TopBar
TitleText.BackgroundTransparency = 1
TitleText.Position = UDim2.new(0, 16, 0, 0)
TitleText.Size = UDim2.new(0, 300, 1, 0)
TitleText.Font = Enum.Font.GothamBold
TitleText.Text = "SENAKA <font color=\"#00DC82\">HUB SYSTEM</font>"
TitleText.RichText = true
TitleText.TextColor3 = Color3.fromRGB(240, 245, 255)
TitleText.TextSize = 13
TitleText.TextXAlignment = Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.BackgroundColor3 = Color3.fromRGB(28, 33, 46)
CloseBtn.Position = UDim2.new(1, -36, 0.5, -11)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "×"
CloseBtn.TextColor3 = Color3.fromRGB(220, 225, 240)
CloseBtn.TextSize = 14

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
ToggleBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Parent = MainFrame
Sidebar.Active = true
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 18, 26)
Sidebar.BorderSizePixel = 0
Sidebar.Position = UDim2.new(0, 0, 0, 42)
Sidebar.Size = UDim2.new(0, 150, 1, -42)
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
Sidebar.AutomaticCanvasSize = Enum.AutomaticSize.Y
Sidebar.ScrollBarThickness = 0

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = Sidebar
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 5)
SidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local SidebarPadding = Instance.new("UIPadding")
SidebarPadding.Parent = Sidebar
SidebarPadding.PaddingTop = UDim.new(0, 10)
SidebarPadding.PaddingLeft = UDim.new(0, 8)
SidebarPadding.PaddingRight = UDim.new(0, 8)

local ContentArea = Instance.new("Frame")
ContentArea.Parent = MainFrame
ContentArea.BackgroundTransparency = 1
ContentArea.Position = UDim2.new(0, 160, 0, 50)
ContentArea.Size = UDim2.new(1, -170, 1, -60)

local Pages = {}
local TabButtons = {}

local function CreatePage(name)
    local scroller = Instance.new("ScrollingFrame")
    scroller.Name = name .. "Page"
    scroller.Parent = ContentArea
    scroller.Active = true
    scroller.BackgroundTransparency = 1
    scroller.Size = UDim2.new(1, 0, 1, 0)
    scroller.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroller.AutomaticCanvasSize = Enum.AutomaticSize.Y
    scroller.ScrollBarThickness = 3
    scroller.ScrollBarImageColor3 = Color3.fromRGB(0, 220, 130)
    scroller.Visible = false

    local layout = Instance.new("UIListLayout")
    layout.Parent = scroller
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 8)

    Pages[name] = scroller
    return scroller
end

local pageMain = CreatePage("Main")
local pageTeleport = CreatePage("Teleport")
pageMain.Visible = true

local function CreateTabButton(text, targetPage, order)
    local btn = Instance.new("TextButton")
    btn.Parent = Sidebar
    btn.BackgroundColor3 = Color3.fromRGB(22, 27, 38)
    btn.BackgroundTransparency = 0.4
    btn.Size = UDim2.new(1, 0, 0, 36)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = "  " .. text
    btn.TextColor3 = Color3.fromRGB(150, 165, 185)
    btn.TextSize = 11.5
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.LayoutOrder = order

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 8)
    btnCorner.Parent = btn

    if targetPage == pageMain then
        btn.BackgroundColor3 = Color3.fromRGB(0, 220, 130)
        btn.BackgroundTransparency = 0.8
        btn.TextColor3 = Color3.fromRGB(0, 245, 150)
    end

    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        for _, b in pairs(TabButtons) do
            b.BackgroundColor3 = Color3.fromRGB(22, 27, 38)
            b.BackgroundTransparency = 0.4
            b.TextColor3 = Color3.fromRGB(150, 165, 185)
        end
        targetPage.Visible = true
        btn.BackgroundColor3 = Color3.fromRGB(0, 220, 130)
        btn.BackgroundTransparency = 0.8
        btn.TextColor3 = Color3.fromRGB(0, 245, 150)
    end)

    table.insert(TabButtons, btn)
end

CreateTabButton("Chức Năng", pageMain, 1)
CreateTabButton("Dịch Chuyển", pageTeleport, 2)

local function AddToggleToPage(page, title, defaultVal, callback)
    local frame = Instance.new("Frame")
    frame.Parent = page
    frame.BackgroundColor3 = Color3.fromRGB(17, 21, 30)
    frame.Size = UDim2.new(1, -6, 0, 40)

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(38, 46, 62)
    stroke.Thickness = 1
    stroke.Parent = frame

    local label = Instance.new("TextLabel")
    label.Parent = frame
    label.BackgroundTransparency = 1
    label.Position = UDim2.new(0, 12, 0, 0)
    label.Size = UDim2.new(1, -65, 1, 0)
    label.Font = Enum.Font.GothamMedium
    label.Text = title
    label.TextColor3 = Color3.fromRGB(225, 230, 242)
    label.TextSize = 11.5
    label.TextXAlignment = Enum.TextXAlignment.Left

    local switchBg = Instance.new("Frame")
    switchBg.Parent = frame
    switchBg.BackgroundColor3 = defaultVal and Color3.fromRGB(0, 220, 130) or Color3.fromRGB(32, 38, 52)
    switchBg.Position = UDim2.new(1, -44, 0.5, -10)
    switchBg.Size = UDim2.new(0, 34, 0, 20)

    local switchCorner = Instance.new("UICorner")
    switchCorner.CornerRadius = UDim.new(1, 0)
    switchCorner.Parent = switchBg

    local circle = Instance.new("Frame")
    circle.Parent = switchBg
    circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    circle.Position = defaultVal and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7)
    circle.Size = UDim2.new(0, 14, 0, 14)

    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(1, 0)
    circleCorner.Parent = circle

    local btn = Instance.new("TextButton")
    btn.Parent = frame
    btn.BackgroundTransparency = 1
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.Text = ""

    local val = defaultVal
    btn.MouseButton1Click:Connect(function()
        val = not val
        TweenService:Create(switchBg, TweenInfo.new(0.2), {BackgroundColor3 = val and Color3.fromRGB(0, 220, 130) or Color3.fromRGB(32, 38, 52)}):Play()
        TweenService:Create(circle, TweenInfo.new(0.2), {Position = val and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7)}):Play()
        pcall(function() callback(val) end)
    end)
end

local function CreateTextBox(page, labelText, defaultText, callbackNum)
    local frame = Instance.new("Frame")
    frame.Parent = page
    frame.BackgroundColor3 = Color3.fromRGB(17, 21, 30)
    frame.Size = UDim2.new(1, -6, 0, 48)

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(38, 46, 62)
    stroke.Thickness = 1
    stroke.Parent = frame

    local label = Instance.new("TextLabel")
    label.Parent = frame
    label.BackgroundTransparency = 1
    label.Position = UDim2.new(0, 12, 0, 3)
    label.Size = UDim2.new(1, -24, 0, 16)
    label.Font = Enum.Font.GothamMedium
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(150, 165, 185)
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left

    local textBox = Instance.new("TextBox")
    textBox.Parent = frame
    textBox.BackgroundColor3 = Color3.fromRGB(9, 12, 17)
    textBox.Position = UDim2.new(0, 12, 0, 22)
    textBox.Size = UDim2.new(1, -24, 0, 20)
    textBox.Font = Enum.Font.GothamMedium
    textBox.Text = tostring(defaultText)
    textBox.TextColor3 = Color3.fromRGB(0, 220, 130)
    textBox.TextSize = 11

    local boxC = Instance.new("UICorner")
    boxC.CornerRadius = UDim.new(0, 5)
    boxC.Parent = textBox
    
    local boxStroke = Instance.new("UIStroke")
    boxStroke.Color = Color3.fromRGB(45, 55, 75)
    boxStroke.Thickness = 1
    boxStroke.Parent = textBox

    textBox.FocusLost:Connect(function(enterPressed)
        if enterPressed then callbackNum(textBox.Text) end
    end)
end

local function AddButtonToPage(page, title, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = page
    btn.BackgroundColor3 = Color3.fromRGB(22, 28, 40)
    btn.Size = UDim2.new(1, -6, 0, 36)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = title
    btn.TextColor3 = Color3.fromRGB(235, 240, 250)
    btn.TextSize = 11.5

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(0, 220, 130)
    stroke.Transparency = 0.4
    stroke.Thickness = 1
    stroke.Parent = btn

    btn.MouseButton1Click:Connect(function() pcall(callback) end)
end

local cfg = _G.Config or {}

CreateTextBox(pageMain, "Nhập Tên Master (Chủ Thuyền):", cfg.MasterName or "bonbicuttay_999", function(txt)
    if _G.Config then _G.Config.MasterName = txt end
end)

CreateTextBox(pageMain, "SelectedBoat (Tên Thuyền Mua):", cfg.SelectedBoat or "Dinghy", function(txt)
    if _G.Config then _G.Config.SelectedBoat = txt end
end)

CreateTextBox(pageMain, "TweenSpeed (Tốc Độ Nhân Vật):", cfg.TweenSpeed or 300, function(txt)
    local num = tonumber(txt)
    if num and _G.Config then _G.Config.TweenSpeed = num end
end)

CreateTextBox(pageMain, "BoatTweenSpeed (Tốc Độ Thuyền):", cfg.BoatTweenSpeed or 350, function(txt)
    local num = tonumber(txt)
    if num and _G.Config then _G.Config.BoatTweenSpeed = num end
end)

CreateTextBox(pageMain, "AutoTravel (Tên đảo/vị trí muốn đến):", cfg.AutoTravel or "", function(txt)
    if _G.Config then _G.Config.AutoTravel = txt end
end)

AddToggleToPage(pageMain, "Role: Master (Chủ thuyền)", cfg.IsMaster or false, function(v) 
    if _G.Config then _G.Config.IsMaster = v end 
end)
AddToggleToPage(pageMain, "Role: Slave (Đi theo Master)", cfg.IsSlave or false, function(v) 
    if _G.Config then _G.Config.IsSlave = v end 
end)
AddToggleToPage(pageMain, "Đứng Trên Mặt Nước", cfg.WaterWalk or false, function(v) 
    if _G.Config then _G.Config.WaterWalk = v end 
end)
AddToggleToPage(pageMain, "Auto Buso (Haki Vũ Trang)", cfg.AutoBuso or false, function(v) 
    if _G.Config then _G.Config.AutoBuso = v end 
end)
AddToggleToPage(pageMain, "Auto Skill V3", cfg.AutoV3 or false, function(v) 
    if _G.Config then _G.Config.AutoV3 = v end 
end)
AddToggleToPage(pageMain, "Auto Skill V4", cfg.AutoV4 or false, function(v) 
    if _G.Config then _G.Config.AutoV4 = v end 
end)
AddToggleToPage(pageMain, "Tự Động Tránh Sea Event", cfg.AutoAvoidSeaEvent or false, function(v) 
    if _G.Config then _G.Config.AutoAvoidSeaEvent = v end 
end)
AddToggleToPage(pageMain, "Auto Sanguine Art / Săn Boss", cfg.AutoGetSanguineArt or false, function(v) 
    if _G.Config then _G.Config.AutoGetSanguineArt = v end 
end)
AddToggleToPage(pageMain, "Kéo Tim Về Tiki", cfg.AutoPullHeartToTiki or false, function(v) 
    if _G.Config then
        _G.Config.AutoPullHeartToTiki = v 
        if v then _G.Config.AutoPullHeartToHydra = false end
    end
end)
AddToggleToPage(pageMain, "Kéo Tim Về Hydra", cfg.AutoPullHeartToHydra or false, function(v) 
    if _G.Config then
        _G.Config.AutoPullHeartToHydra = v 
        if v then _G.Config.AutoPullHeartToTiki = false end
    end
end)

AddButtonToPage(pageTeleport, "Dịch Chuyển Đến Tiki Outpost", function()
    if _G.SenakaCharacterMove and _G.SenakaLocations and _G.Config then
        _G.SenakaCharacterMove(_G.SenakaLocations.TikiBoat, _G.Config.TweenSpeed)
    end
end)

AddButtonToPage(pageTeleport, "Dịch Chuyển Đến Hydra Island", function()
    if _G.SenakaCharacterMove and _G.SenakaLocations and _G.Config then
        _G.SenakaCharacterMove(_G.SenakaLocations.Hydra, _G.Config.TweenSpeed)
    end
end)

AddButtonToPage(pageTeleport, "Dịch Chuyển Đến Spy NPC", function()
    if _G.SenakaCharacterMove and _G.SenakaLocations and _G.Config then
        _G.SenakaCharacterMove(_G.SenakaLocations.SpyNpc, _G.Config.TweenSpeed)
    end
end)
