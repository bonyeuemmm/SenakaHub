local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer
local VirtualInputManager = game:GetService("VirtualInputManager")

task.spawn(function()
    pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("SetTeam", "Marines") end)
    pcall(function()
        if getrawmetatable and newcclosure then
            local rawMeta = getrawmetatable(game)
            if rawMeta then
                if setreadonly then setreadonly(rawMeta, false) end
                local oldNamecall = rawMeta.__namecall
                rawMeta.__namecall = newcclosure(function(self, ...)
                    local method = getnamecallmethod and getnamecallmethod()
                    if method == "FireServer" or method == "InvokeServer" then
                        local nameStr = tostring(self)
                        if nameStr:find("Ban") or nameStr:find("Check") or nameStr:find("Admin") or nameStr:find("Detection") then return nil end
                    end
                    if method == "Kick" or method == "kick" then return nil end
                    return oldNamecall(self, ...)
                end)
                if setreadonly then setreadonly(rawMeta, true) end
            end
        end
    end)
end)

task.spawn(function()
    pcall(function()
        Lighting.FogEnd = 999999
        Lighting.GlobalShadows = false
        Lighting.Brightness = 2
        for _, v in pairs(Lighting:GetChildren()) do
            if v:IsA("Atmosphere") or v:IsA("PostEffect") or v:IsA("Sky") then v:Destroy() end
        end
    end)
end)
_G.Config = {
    IsMaster = false, 
    IsSlave = false, 
    MasterName = "bonbicuttay_999",
    WaterWalk = true,
    AutoBuso = true,
    AutoAvoidSeaEvent = true,
    SelectedBoat = "Dinghy",
    AutoPullHeartToTiki = false,
    AutoPullHeartToHydra = false,
    AutoTravel = "",
    TweenSpeed = 300,
    BoatTweenSpeed = 350
}

_G.BoatList = {"Dinghy", "Sloop", "Caravel", "Galleon", "Swan", "Miracle"}

_G.SenakaLocations = {
    TikiBoat = CFrame.new(-16234, 25, 416),
    Hydra = CFrame.new(5749, 610, -282),
    SpyNpc = CFrame.new(-16110, 20, 450),
    SeaSearchCenter = CFrame.new(-16000, 20, -15000)
}

_G.SenakaCharacterMove = function(targetCFrame, speed)
    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not root or not hum then return end

    speed = speed or _G.Config.TweenSpeed
    local noclipConn = RunService.Stepped:Connect(function()
        if char and char.Parent then
            for _, part in pairs(char:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)

    hum.PlatformStand = true
    local distance = (root.Position - targetCFrame.Position).Magnitude
    local duration = distance / speed
    local tween = TweenService:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = targetCFrame})

    tween:Play()
    tween.Completed:Wait()
    if noclipConn then noclipConn:Disconnect() end
    hum.PlatformStand = false
end

local function GetPlayerBoat()
    local boatsFolder = workspace:FindFirstChild("Boats")
    if boatsFolder then
        for _, boat in pairs(boatsFolder:GetChildren()) do
            local owner = boat:FindFirstChild("Owner")
            if owner then
                if typeof(owner.Value) == "Instance" and owner.Value == LocalPlayer then return boat end
                if tostring(owner.Value) == LocalPlayer.Name then return boat end
            end
        end
    end
    return nil
end

local lastBuyTime = 0
local function FindAndInteractBoatDealer()
    if tick() - lastBuyTime < 10 then return end
    
    local dealerPart = nil
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("Model") then
            local nameLower = obj.Name:lower()
            if nameLower:find("boat dealer") or nameLower:find("boatdealer") or nameLower:find("dealer") then
                if obj:FindFirstChild("HumanoidRootPart") then
                    dealerPart = obj.HumanoidRootPart
                    break
                end
            end
        end
    end
    
    if not dealerPart then
        for _, obj in pairs(workspace:GetChildren()) do
            if obj:IsA("Model") and obj:FindFirstChild("HumanoidRootPart") and obj:FindFirstChild("Humanoid") then
                local dist = (obj.HumanoidRootPart.Position - _G.SenakaLocations.TikiBoat.Position).Magnitude
                if dist < 300 then
                    dealerPart = obj.HumanoidRootPart
                    break
                end
            end
        end
    end

    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if dealerPart and root then
        if (dealerPart.Position - root.Position).Magnitude > 10 then
            _G.SenakaCharacterMove(dealerPart.CFrame * CFrame.new(0, 3, 3), _G.Config.TweenSpeed)
            task.wait(0.5)
        end
    end

    pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("BuyBoat", _G.Config.SelectedBoat or "Dinghy") end)
    lastBuyTime = tick()
    task.wait(3)
end

local processedBoats = {}
local function SecureBoatPhysics(boatModel)
    if not boatModel or processedBoats[boatModel] then return end
    processedBoats[boatModel] = true
    task.spawn(function()
        task.wait(0.5)
        pcall(function()
            for _, part in pairs(boatModel:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = true end
            end
        end)
    end)
end

local function CountPlayersOnBoat(boatModel)
    if not boatModel then return 0 end
    local count = 0
    local countedPlayers = {}
    for _, desc in pairs(boatModel:GetDescendants()) do
        if desc:IsA("Seat") or desc:IsA("VehicleSeat") then
            if desc.Occupant and desc.Occupant.Parent then
                local player = Players:GetPlayerFromCharacter(desc.Occupant.Parent)
                if player and not countedPlayers[player] then
                    countedPlayers[player] = true
                    count = count + 1
                end
            end
        end
    end
    return count
end

local WaterPlatform = Instance.new("Part")
WaterPlatform.Name = "SenakaWaterSurfacePlatform"
WaterPlatform.Size = Vector3.new(100, 1, 100)
WaterPlatform.Anchored = true
WaterPlatform.Transparency = 1
WaterPlatform.CanCollide = false
WaterPlatform.Parent = workspace

RunService.RenderStepped:Connect(function()
    if _G.Config.WaterWalk then
        pcall(function()
            local char = LocalPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChild("Humanoid")
            
            if root and hum and not hum.SeatPart then
                if root.Position.Y <= 10 and root.Position.Y >= -30 then
                    WaterPlatform.CFrame = CFrame.new(root.Position.X, -2.5, root.Position.Z)
                    WaterPlatform.CanCollide = true
                else
                    WaterPlatform.CanCollide = false
                end
            else
                WaterPlatform.CanCollide = false
            end
        end)
    else
        WaterPlatform.CanCollide = false
    end
end)
task.spawn(function()
    while task.wait(1) do
        if _G.Config.AutoBuso then
            pcall(function()
                local char = LocalPlayer.Character
                if char and not char:FindFirstChild("HasBuso") then ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso") end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.3) do
        if _G.Config.AutoAvoidSeaEvent then
            pcall(function()
                local char = LocalPlayer.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if root then
                    local seaMonsters = {"SeaBeast", "TerrorShark", "Piranha", "ShipRaid", "SeaMonster"}
                    for _, obj in pairs(workspace:GetChildren()) do
                        for _, name in ipairs(seaMonsters) do
                            if obj.Name:find(name) then
                                local monsterPart = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
                                if monsterPart and (monsterPart.Position - root.Position).Magnitude < 300 then
                                    root.CFrame = root.CFrame + Vector3.new(0, 150, 0)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        local Heart = workspace:FindFirstChild("LeviathanHeart")
        local Root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if Heart and Root then
            if _G.Config.AutoPullHeartToTiki then
                Heart:PivotTo(_G.SenakaLocations.TikiBoat + Vector3.new(0, 10, 0))
                pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end)
            elseif _G.Config.AutoPullHeartToHydra then
                Heart:PivotTo(_G.SenakaLocations.Hydra + Vector3.new(0, 10, 0))
                pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end)
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.Config.AutoTravel ~= "" then
            pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("Travel", _G.Config.AutoTravel) end)
            _G.Config.AutoTravel = ""
        end
    end
end)

local boatFlightState = "Idle" 

task.spawn(function()
    while task.wait(0.5) do
        local Char = LocalPlayer.Character
        local Hum = Char and Char:FindFirstChild("Humanoid")
        local Root = Char and Char:FindFirstChild("HumanoidRootPart")

        if _G.Config.IsMaster and Root and Hum and Hum.Health > 0 then
            local IceIsland = workspace:FindFirstChild("LeviathanIceIsland")
            local Heart = workspace:FindFirstChild("LeviathanHeart")
            local distToTiki = (Root.Position - _G.SenakaLocations.TikiBoat.Position).Magnitude
            
            if distToTiki > 150 and not GetPlayerBoat() and not IceIsland then
                _G.SenakaCharacterMove(_G.SenakaLocations.TikiBoat, _G.Config.TweenSpeed)
                boatFlightState = "Idle"
            else
                local myBoat = GetPlayerBoat()
                if not myBoat then
                    FindAndInteractBoatDealer()
                    boatFlightState = "Idle"
                else
                    SecureBoatPhysics(myBoat)
                    
                    if not Hum.SeatPart then
                        local targetSeat = nil
                        for _, d in pairs(myBoat:GetDescendants()) do
                            if d:IsA("VehicleSeat") and not d.Occupant then
                                targetSeat = d
                                break
                            end
                        end
                        if not targetSeat then
                            for _, d in pairs(myBoat:GetDescendants()) do
                                if d:IsA("Seat") and not d.Occupant then
                                    targetSeat = d
                                    break
                                end
                            end
                        end

                        if targetSeat then
                            Hum.PlatformStand = false
                            Root.CFrame = targetSeat.CFrame
                            task.wait(0.3)
                            pcall(function() targetSeat:Sit(Hum) end)
                        else
                            local boatPart = myBoat.PrimaryPart or myBoat:FindFirstChildWhichIsA("BasePart")
                            if boatPart and (Root.Position - boatPart.Position).Magnitude > 15 then
                                Root.CFrame = boatPart.CFrame + Vector3.new(0, 5, 0)
                            end
                        end
                    else
                        if CountPlayersOnBoat(myBoat) >= 5 then
                            if IceIsland then
                                local islandPart = IceIsland:FindFirstChild("PrimaryPart") or IceIsland:FindFirstChildWhichIsA("BasePart")
                                if islandPart then
                                    if (Root.Position - islandPart.Position).Magnitude > 150 then
                                        _G.SenakaCharacterMove(islandPart.CFrame * CFrame.new(0, 20, 0), _G.Config.TweenSpeed)
                                    else
                                        _G.SenakaCharacterMove(_G.SenakaLocations.SpyNpc, _G.Config.TweenSpeed)
                                        task.wait(0.5)
                                        pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("FindLeviathan") end)
                                    end
                                end
                                if Heart then pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("PullHeart") end) end
                            else
                                local boatPrimary = myBoat.PrimaryPart or myBoat:FindFirstChildWhichIsA("BasePart")
                                if boatPrimary then
                                    local distanceToSea = (boatPrimary.Position - _G.SenakaLocations.SeaSearchCenter.Position).Magnitude
                                    
                                    if boatFlightState == "Idle" then
                                        local highTarget = CFrame.new(boatPrimary.Position.X, 150, boatPrimary.Position.Z)
                                        local upTween = TweenService:Create(boatPrimary, TweenInfo.new(1.5, Enum.EasingStyle.Linear), {CFrame = highTarget})
                                        upTween:Play()
                                        upTween.Completed:Wait()
                                        boatFlightState = "FlyingToSea"
                                    elseif boatFlightState == "FlyingToSea" then
                                        if distanceToSea > 300 then
                                            local highSeaTarget = CFrame.new(_G.SenakaLocations.SeaSearchCenter.Position.X, 150, _G.SenakaLocations.SeaSearchCenter.Position.Z)
                                            local duration = (boatPrimary.Position - highSeaTarget.Position).Magnitude / (_G.Config.BoatTweenSpeed or 350)
                                            local seaTween = TweenService:Create(boatPrimary, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = highSeaTarget})
                                            seaTween:Play()
                                            seaTween.Completed:Wait()
                                        end
                                        boatFlightState = "Lowering"
                                    elseif boatFlightState == "Lowering" then
                                        local lowSeaTarget = CFrame.new(_G.SenakaLocations.SeaSearchCenter.Position.X, 5, _G.SenakaLocations.SeaSearchCenter.Position.Z)
                                        local downTween = TweenService:Create(boatPrimary, TweenInfo.new(1, Enum.EasingStyle.Linear), {CFrame = lowSeaTarget})
                                        downTween:Play()
                                        downTween.Completed:Wait()
                                        boatFlightState = "Searching"
                                    elseif boatFlightState == "Searching" then
                                        pcall(function() ReplicatedStorage.Remotes.CommF_:InvokeServer("FindLeviathan") end)
                                    end
                                end
                            end
                        else
                            boatFlightState = "Idle"
                        end
                    end
                end
            end
        end

        if _G.Config.IsSlave and Root and Hum and Hum.Health > 0 then
            local MasterPlayer = nil
            local foundMaster = false
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and (p.Name:lower() == _G.Config.MasterName:lower() or p.DisplayName:lower() == _G.Config.MasterName:lower()) then
                    MasterPlayer = p
                    foundMaster = true
                    break
                end
            end
            
            if not foundMaster and _G.Config.MasterName ~= "" then
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer then
                        MasterPlayer = p
                        break
                    end
                end
            end

            if MasterPlayer and MasterPlayer.Character then
                local MasterRoot = MasterPlayer.Character:FindFirstChild("HumanoidRootPart")
                if MasterRoot and not Hum.SeatPart then
                    local TargetSeat = nil
                    local masterBoat = GetPlayerBoat()
                    if masterBoat then
                        for _, d in pairs(masterBoat:GetDescendants()) do
                            if (d:IsA("Seat") or d:IsA("VehicleSeat")) and not d.Occupant then
                                TargetSeat = d
                                break
                            end
                        end
                    end

                    if TargetSeat then
                        Hum.PlatformStand = false
                        Root.CFrame = TargetSeat.CFrame
                        task.wait(0.3)
                        pcall(function() TargetSeat:Sit(Hum) end)
                    else
                        if (Root.Position - MasterRoot.Position).Magnitude > 20 then
                            Root.CFrame = MasterRoot.CFrame * CFrame.new(0, 0, 5)
                        end
                    end
                end
            end
        end
    end
end)
pcall(function()
    if CoreGui:FindFirstChild("SenakaPremiumUI") then CoreGui.SenakaPremiumUI:Destroy() end
end)

local targetParent = CoreGui
pcall(function()
    if gethui then targetParent = gethui()
    elseif LocalPlayer and LocalPlayer:FindFirstChild("PlayerGui") then targetParent = LocalPlayer.PlayerGui end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SenakaPremiumUI"
ScreenGui.Parent = targetParent
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

local ToggleMenuBtn = Instance.new("ImageButton")
ToggleMenuBtn.Name = "ToggleMenuBtn"
ToggleMenuBtn.Parent = ScreenGui
ToggleMenuBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
ToggleMenuBtn.Position = UDim2.new(0, 15, 0, 150)
ToggleMenuBtn.Size = UDim2.new(0, 45, 0, 45)
ToggleMenuBtn.Image = "rbxassetid://130284374965787"
local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 10)
ToggleCorner.Parent = ToggleMenuBtn

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -180)
MainFrame.Size = UDim2.new(0, 480, 0, 360)
local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local TitleText = Instance.new("TextLabel")
TitleText.Parent = MainFrame
TitleText.BackgroundTransparency = 1
TitleText.Position = UDim2.new(0, 15, 0, 10)
TitleText.Size = UDim2.new(1, -30, 0, 20)
TitleText.Font = Enum.Font.GothamMedium
TitleText.Text = "Senaka Premium [ Blox Fruits ]"
TitleText.TextColor3 = Color3.fromRGB(200, 200, 200)
TitleText.TextSize = 12
TitleText.TextXAlignment = Enum.TextXAlignment.Right

local TabContainer = Instance.new("Frame")
TabContainer.Parent = MainFrame
TabContainer.BackgroundTransparency = 1
TabContainer.Position = UDim2.new(0, 15, 0, 35)
TabContainer.Size = UDim2.new(1, -30, 0, 30)

local MainTabBtn = Instance.new("TextButton")
MainTabBtn.Parent = TabContainer
MainTabBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 245)
MainTabBtn.Size = UDim2.new(0.5, -5, 1, 0)
MainTabBtn.Font = Enum.Font.GothamBold
MainTabBtn.Text = "Main Settings"
MainTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MainTabBtn.TextSize = 13
local MCorner = Instance.new("UICorner")
MCorner.CornerRadius = UDim.new(0, 6)
MCorner.Parent = MainTabBtn

local TeleportTabBtn = Instance.new("TextButton")
TeleportTabBtn.Parent = TabContainer
TeleportTabBtn.Position = UDim2.new(0.5, 5, 0, 0)
TeleportTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
TeleportTabBtn.Size = UDim2.new(0.5, -5, 1, 0)
TeleportTabBtn.Font = Enum.Font.GothamBold
TeleportTabBtn.Text = "Teleport"
TeleportTabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
TeleportTabBtn.TextSize = 13
local TCorner = Instance.new("UICorner")
TCorner.CornerRadius = UDim.new(0, 6)
TCorner.Parent = TeleportTabBtn

local MainScroller = Instance.new("ScrollingFrame")
MainScroller.Parent = MainFrame
MainScroller.Active = true
MainScroller.BackgroundTransparency = 1
MainScroller.Position = UDim2.new(0, 15, 0, 75)
MainScroller.Size = UDim2.new(1, -30, 1, -85)
MainScroller.CanvasSize = UDim2.new(0, 0, 0, 0)
MainScroller.AutomaticCanvasSize = Enum.AutomaticSize.Y
MainScroller.ScrollBarThickness = 3
MainScroller.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 70)
local MainLayout = Instance.new("UIListLayout")
MainLayout.Parent = MainScroller
MainLayout.SortOrder = Enum.SortOrder.LayoutOrder
MainLayout.Padding = UDim.new(0, 8)

local TeleportScroller = Instance.new("ScrollingFrame")
TeleportScroller.Parent = MainFrame
TeleportScroller.Active = true
TeleportScroller.BackgroundTransparency = 1
TeleportScroller.Position = UDim2.new(0, 15, 0, 75)
TeleportScroller.Size = UDim2.new(1, -30, 1, -85)
TeleportScroller.CanvasSize = UDim2.new(0, 0, 0, 0)
TeleportScroller.AutomaticCanvasSize = Enum.AutomaticSize.Y
TeleportScroller.ScrollBarThickness = 3
TeleportScroller.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 70)
TeleportScroller.Visible = false
local TeleportLayout = Instance.new("UIListLayout")
TeleportLayout.Parent = TeleportScroller
TeleportLayout.SortOrder = Enum.SortOrder.LayoutOrder
TeleportLayout.Padding = UDim.new(0, 8)

MainTabBtn.MouseButton1Click:Connect(function()
    MainScroller.Visible = true
    TeleportScroller.Visible = false
    MainTabBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 245)
    MainTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    TeleportTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    TeleportTabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
end)

TeleportTabBtn.MouseButton1Click:Connect(function()
    MainScroller.Visible = false
    TeleportScroller.Visible = true
    TeleportTabBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 245)
    TeleportTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    MainTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    MainTabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
end)

ToggleMenuBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

-- UI COMPONENTS
local function AddSection(parentScroller, text)
    local Label = Instance.new("TextLabel")
    Label.Parent = parentScroller
    Label.BackgroundTransparency = 1
    Label.Size = UDim2.new(1, 0, 0, 28)
    Label.Font = Enum.Font.GothamBold
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextSize = 17
    Label.TextXAlignment = Enum.TextXAlignment.Left
end

local function AddToggleCard(parentScroller, title, desc, defaultVal, callback)
    local Card = Instance.new("TextButton")
    Card.Parent = parentScroller
    Card.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    Card.Size = UDim2.new(1, 0, 0, 60)
    Card.Text = ""
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Card
    
    local TxtTitle = Instance.new("TextLabel")
    TxtTitle.Parent = Card
    TxtTitle.BackgroundTransparency = 1
    TxtTitle.Position = UDim2.new(0, 15, 0, 10)
    TxtTitle.Size = UDim2.new(1, -70, 0, 18)
    TxtTitle.Font = Enum.Font.GothamBold
    TxtTitle.Text = title
    TxtTitle.TextColor3 = Color3.fromRGB(230, 230, 230)
    TxtTitle.TextSize = 13
    TxtTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    local TxtDesc = Instance.new("TextLabel")
    TxtDesc.Parent = Card
    TxtDesc.BackgroundTransparency = 1
    TxtDesc.Position = UDim2.new(0, 15, 0, 30)
    TxtDesc.Size = UDim2.new(1, -70, 0, 18)
    TxtDesc.Font = Enum.Font.Gotham
    TxtDesc.Text = desc
    TxtDesc.TextColor3 = Color3.fromRGB(130, 130, 130)
    TxtDesc.TextSize = 11
    TxtDesc.TextXAlignment = Enum.TextXAlignment.Left
    
    local Circle = Instance.new("Frame")
    Circle.Parent = Card
    Circle.BackgroundColor3 = defaultVal and Color3.fromRGB(60, 130, 245) or Color3.fromRGB(30, 30, 35)
    Circle.Position = UDim2.new(1, -38, 0.5, -11)
    Circle.Size = UDim2.new(0, 22, 0, 22)
    local CircleCorner = Instance.new("UICorner")
    CircleCorner.CornerRadius = UDim.new(1, 0)
    CircleCorner.Parent = Circle
    
    local isToggled = defaultVal
    Card.MouseButton1Click:Connect(function()
        isToggled = not isToggled
        TweenService:Create(Circle, TweenInfo.new(0.2), {BackgroundColor3 = isToggled and Color3.fromRGB(60, 130, 245) or Color3.fromRGB(30, 30, 35)}):Play()
        pcall(function() callback(isToggled) end)
    end)
end

local function AddInputCard(parentScroller, title, desc, defaultText, callback)
    local Card = Instance.new("Frame")
    Card.Parent = parentScroller
    Card.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    Card.Size = UDim2.new(1, 0, 0, 60)
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Card
    
    local TxtTitle = Instance.new("TextLabel")
    TxtTitle.Parent = Card
    TxtTitle.BackgroundTransparency = 1
    TxtTitle.Position = UDim2.new(0, 15, 0, 10)
    TxtTitle.Size = UDim2.new(0.5, 0, 0, 18)
    TxtTitle.Font = Enum.Font.GothamBold
    TxtTitle.Text = title
    TxtTitle.TextColor3 = Color3.fromRGB(230, 230, 230)
    TxtTitle.TextSize = 13
    TxtTitle.TextXAlignment = Enum.TextXAlignment.Left

    local TxtDesc = Instance.new("TextLabel")
    TxtDesc.Parent = Card
    TxtDesc.BackgroundTransparency = 1
    TxtDesc.Position = UDim2.new(0, 15, 0, 30)
    TxtDesc.Size = UDim2.new(0.5, 0, 0, 18)
    TxtDesc.Font = Enum.Font.Gotham
    TxtDesc.Text = desc
    TxtDesc.TextColor3 = Color3.fromRGB(130, 130, 130)
    TxtDesc.TextSize = 11
    TxtDesc.TextXAlignment = Enum.TextXAlignment.Left
    
    local TextBox = Instance.new("TextBox")
    TextBox.Parent = Card
    TextBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    TextBox.Position = UDim2.new(1, -120, 0.5, -14)
    TextBox.Size = UDim2.new(0, 105, 0, 28)
    TextBox.Font = Enum.Font.GothamMedium
    TextBox.Text = tostring(defaultText)
    TextBox.TextColor3 = Color3.fromRGB(200, 200, 200)
    TextBox.TextSize = 12
    local BoxCorner = Instance.new("UICorner")
    BoxCorner.CornerRadius = UDim.new(0, 5)
    BoxCorner.Parent = TextBox
    
    TextBox.FocusLost:Connect(function(enterPressed)
        if enterPressed then callback(TextBox.Text) end
    end)
end

local function AddCycleCard(parentScroller, title, desc, options, defaultVal, callback)
    local Card = Instance.new("TextButton")
    Card.Parent = parentScroller
    Card.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    Card.Size = UDim2.new(1, 0, 0, 60)
    Card.Text = ""
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Card
    
    local TxtTitle = Instance.new("TextLabel")
    TxtTitle.Parent = Card
    TxtTitle.BackgroundTransparency = 1
    TxtTitle.Position = UDim2.new(0, 15, 0, 10)
    TxtTitle.Size = UDim2.new(0.5, 0, 0, 18)
    TxtTitle.Font = Enum.Font.GothamBold
    TxtTitle.Text = title
    TxtTitle.TextColor3 = Color3.fromRGB(230, 230, 230)
    TxtTitle.TextSize = 13
    TxtTitle.TextXAlignment = Enum.TextXAlignment.Left

    local TxtDesc = Instance.new("TextLabel")
    TxtDesc.Parent = Card
    TxtDesc.BackgroundTransparency = 1
    TxtDesc.Position = UDim2.new(0, 15, 0, 30)
    TxtDesc.Size = UDim2.new(0.5, 0, 0, 18)
    TxtDesc.Font = Enum.Font.Gotham
    TxtDesc.Text = desc
    TxtDesc.TextColor3 = Color3.fromRGB(130, 130, 130)
    TxtDesc.TextSize = 11
    TxtDesc.TextXAlignment = Enum.TextXAlignment.Left
    
    local ValueBox = Instance.new("Frame")
    ValueBox.Parent = Card
    ValueBox.BackgroundColor3 = Color3.fromRGB(60, 130, 245)
    ValueBox.Position = UDim2.new(1, -120, 0.5, -14)
    ValueBox.Size = UDim2.new(0, 105, 0, 28)
    local VCorner = Instance.new("UICorner")
    VCorner.CornerRadius = UDim.new(0, 5)
    VCorner.Parent = ValueBox
    
    local ValueText = Instance.new("TextLabel")
    ValueText.Parent = ValueBox
    ValueText.BackgroundTransparency = 1
    ValueText.Size = UDim2.new(1, 0, 1, 0)
    ValueText.Font = Enum.Font.GothamBold
    ValueText.Text = tostring(defaultVal)
    ValueText.TextColor3 = Color3.fromRGB(255, 255, 255)
    ValueText.TextSize = 12
    
    local currentIndex = table.find(options, defaultVal) or 1
    
    Card.MouseButton1Click:Connect(function()
        currentIndex = currentIndex + 1
        if currentIndex > #options then currentIndex = 1 end
        ValueText.Text = options[currentIndex]
        pcall(function() callback(options[currentIndex]) end)
    end)
end

local function AddButtonCard(parentScroller, title, desc, callback)
    local Card = Instance.new("TextButton")
    Card.Parent = parentScroller
    Card.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    Card.Size = UDim2.new(1, 0, 0, 50)
    Card.Text = ""
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Card
    
    local TxtTitle = Instance.new("TextLabel")
    TxtTitle.Parent = Card
    TxtTitle.BackgroundTransparency = 1
    TxtTitle.Position = UDim2.new(0, 15, 0, 7)
    TxtTitle.Size = UDim2.new(1, -30, 0, 18)
    TxtTitle.Font = Enum.Font.GothamBold
    TxtTitle.Text = title
    TxtTitle.TextColor3 = Color3.fromRGB(230, 230, 230)
    TxtTitle.TextSize = 13
    TxtTitle.TextXAlignment = Enum.TextXAlignment.Left

    local TxtDesc = Instance.new("TextLabel")
    TxtDesc.Parent = Card
    TxtDesc.BackgroundTransparency = 1
    TxtDesc.Position = UDim2.new(0, 15, 0, 25)
    TxtDesc.Size = UDim2.new(1, -30, 0, 18)
    TxtDesc.Font = Enum.Font.Gotham
    TxtDesc.Text = desc
    TxtDesc.TextColor3 = Color3.fromRGB(130, 130, 130)
    TxtDesc.TextSize = 11
    TxtDesc.TextXAlignment = Enum.TextXAlignment.Left
    
    Card.MouseButton1Click:Connect(function() pcall(callback) end)
end

local cfg = _G.Config

AddSection(MainScroller, "Player & Utility")
AddToggleCard(MainScroller, "Auto Buso Haki", "Automatically activates Buso Haki.", cfg.AutoBuso, function(v) cfg.AutoBuso = v end)
AddToggleCard(MainScroller, "Water Walk", "Walk on water surface smoothly.", cfg.WaterWalk, function(v) cfg.WaterWalk = v end)

AddSection(MainScroller, "Leviathan System")
AddToggleCard(MainScroller, "Role: Master", "Host the boat and drive to Leviathan.", cfg.IsMaster, function(v) cfg.IsMaster = v end)
AddToggleCard(MainScroller, "Role: Slave", "Automatically follows and sits on Master's boat.", cfg.IsSlave, function(v) cfg.IsSlave = v end)
AddToggleCard(MainScroller, "Avoid Sea Event", "Automatically flies up to dodge Sea Event mobs.", cfg.AutoAvoidSeaEvent, function(v) cfg.AutoAvoidSeaEvent = v end)
AddToggleCard(MainScroller, "Pull Heart To Tiki", "Takes Leviathan heart back to Tiki Outpost.", cfg.AutoPullHeartToTiki, function(v) 
    cfg.AutoPullHeartToTiki = v; if v then cfg.AutoPullHeartToHydra = false end 
end)
AddToggleCard(MainScroller, "Pull Heart To Hydra", "Takes Leviathan heart back to Hydra Island.", cfg.AutoPullHeartToHydra, function(v) 
    cfg.AutoPullHeartToHydra = v; if v then cfg.AutoPullHeartToTiki = false end 
end)

AddSection(MainScroller, "Configurations")
AddInputCard(MainScroller, "Master Name", "Enter host player name", cfg.MasterName, function(t) cfg.MasterName = t end)
AddCycleCard(MainScroller, "Select Boat", "Click to switch boat type", _G.BoatList, cfg.SelectedBoat, function(t) cfg.SelectedBoat = t end)
AddInputCard(MainScroller, "Player Speed", "Character tween speed", cfg.TweenSpeed, function(t) if tonumber(t) then cfg.TweenSpeed = tonumber(t) end end)
AddInputCard(MainScroller, "Boat Speed", "Boat tween speed", cfg.BoatTweenSpeed, function(t) if tonumber(t) then cfg.BoatTweenSpeed = tonumber(t) end end)

AddSection(TeleportScroller, "Auto Travel")
AddInputCard(TeleportScroller, "Auto Travel", "Type island name to teleport", cfg.AutoTravel, function(t) cfg.AutoTravel = t end)

AddSection(TeleportScroller, "Quick Locations")
AddButtonCard(TeleportScroller, "Teleport to Tiki", "Instant tween to Tiki Outpost.", function() _G.SenakaCharacterMove(_G.SenakaLocations.TikiBoat, _G.Config.TweenSpeed) end)
AddButtonCard(TeleportScroller, "Teleport to Hydra", "Instant tween to Hydra Island.", function() _G.SenakaCharacterMove(_G.SenakaLocations.Hydra, _G.Config.TweenSpeed) end)
AddButtonCard(TeleportScroller, "Teleport to Spy NPC", "Instant tween to Spy NPC location.", function() _G.SenakaCharacterMove(_G.SenakaLocations.SpyNpc, _G.Config.TweenSpeed) end)
